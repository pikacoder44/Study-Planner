import Link from "next/link";
import { BookOpen, CalendarDays, Check, Clock3, GraduationCap, MapPin, Plus } from "lucide-react";
import AppShell from "@/components/AppShell";
import TaskList from "@/components/TaskList";
import { Badge, Button, Card, PageHeader } from "@/components/ui";
import { classes, currentUser, exams, subjects, studySessions, tasks } from "@/lib/mock-data";

const todaySchedule = [
  ["09:00", "Software Architecture", "Independent review", "Study"],
  ["11:00", "Database Systems", "Room 204", "Class"],
  ["14:00", "Architecture assignment", "Due tomorrow", "Task"],
] as const;

export default function DashboardPage() {
  const pendingTasks = tasks.filter((task) => task.status === "pending");
  const upcomingExams = exams.filter((exam) => exam.examDate >= "2026-08-19").slice(0, 2);
  const upcomingClasses = classes.slice(0, 3);

  return <AppShell><PageHeader eyebrow="Wednesday, August 19" title={`Good morning, ${currentUser.name.split(" ")[0]}`} description="Here is what you have planned today, and what deserves your attention next." action={<Link href="/tasks/new"><Button><Plus size={17} />New task</Button></Link>} /><div className="mb-10 grid border-y border-[var(--border)] sm:grid-cols-3"><QuickStat label="Pending tasks" value={`${pendingTasks.length}`} detail="1 needs attention today" /><QuickStat label="Upcoming exams" value={`${upcomingExams.length}`} detail="Next one tomorrow" /><QuickStat label="Study time" value="9h 45m" detail="Across 4 sessions" /></div><div className="grid gap-10 xl:grid-cols-[1.15fr_0.85fr]"><div className="space-y-10"><DashboardSection title="Today&apos;s schedule" icon={<CalendarDays size={18} />}><div className="divide-y divide-[var(--border)] border-y border-[var(--border)]">{todaySchedule.map(([time, title, detail, type]) => <div key={title} className="grid grid-cols-[4.5rem_1fr_auto] items-center gap-4 py-4"><span className="text-sm font-bold text-[var(--accent)]">{time}</span><div><p className="text-sm font-bold">{title}</p><p className="mt-1 text-xs text-[var(--muted)]">{detail}</p></div><Badge tone={type === "Task" ? "amber" : type === "Class" ? "blue" : "neutral"}>{type}</Badge></div>)}</div></DashboardSection><DashboardSection title="Tasks to complete" icon={<Check size={18} />} action={<Link href="/tasks" className="text-sm font-semibold text-[var(--accent)]">View all</Link>}><TaskList limit={3} /></DashboardSection><DashboardSection title="Recent study activity" icon={<Clock3 size={18} />} action={<Link href="/study-sessions" className="text-sm font-semibold text-[var(--accent)]">See history</Link>}><div className="divide-y divide-[var(--border)] border-y border-[var(--border)]">{studySessions.slice(0, 3).map((session) => <div key={session.id} className="flex items-center gap-4 py-4"><span className="flex h-9 w-9 items-center justify-center rounded-md bg-[var(--amber-soft)] text-[var(--accent)]"><BookOpen size={17} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{session.title}</p><p className="mt-1 text-xs text-[var(--muted)]">{subjects.find((subject) => subject.id === session.subjectId)?.code} · {session.date}</p></div><span className="text-sm font-semibold text-[var(--muted)]">{session.startTime} - {session.endTime}</span></div>)}</div></DashboardSection></div><aside className="space-y-8"><DashboardSection title="Upcoming exams" icon={<GraduationCap size={18} />} action={<Link href="/exams" className="text-sm font-semibold text-[var(--accent)]">View all</Link>}><div className="space-y-3">{upcomingExams.map((exam, index) => <Card key={exam.id} className="border-l-4 border-l-[var(--amber)] p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold text-[var(--accent)]">{subjects.find((subject) => subject.id === exam.subjectId)?.code}</p><p className="mt-1 text-sm font-bold">{exam.title}</p></div><Badge tone={index === 0 ? "red" : "amber"}>{index === 0 ? "Tomorrow" : "9 days"}</Badge></div><p className="mt-3 text-xs text-[var(--muted)]">{exam.examDate} · {exam.startTime} · {exam.location}</p></Card>)}</div></DashboardSection><DashboardSection title="Upcoming classes" icon={<GraduationCap size={18} />} action={<Link href="/classes" className="text-sm font-semibold text-[var(--accent)]">Timetable</Link>}><div className="divide-y divide-[var(--border)] border-y border-[var(--border)]">{upcomingClasses.map((item) => { const subject = subjects.find((subjectItem) => subjectItem.id === item.subjectId); return <div key={item.id} className="py-4"><div className="flex items-center justify-between"><p className="text-sm font-bold">{subject?.code}</p><span className="text-xs font-semibold text-[var(--accent)]">{item.dayOfWeek}</span></div><p className="mt-1 text-sm">{subject?.name}</p><p className="mt-2 flex items-center gap-1 text-xs text-[var(--muted)]"><Clock3 size={13} />{item.startTime} - {item.endTime}<span className="mx-1">·</span><MapPin size={13} />{item.room}</p></div>; })}</div></DashboardSection><DashboardSection title="Study progress" icon={<Clock3 size={18} />}><div className="border-y border-[var(--border)] py-4"><div className="flex items-end justify-between"><div><p className="text-3xl font-bold tracking-[-0.04em]">9h 45m</p><p className="mt-1 text-xs text-[var(--muted)]">Focused study this week</p></div><span className="text-sm font-bold text-[#557149]">+2h</span></div><div className="mt-5 h-2 rounded-full bg-[#edf0ed]"><div className="h-full w-[68%] rounded-full bg-[var(--amber)]" /></div><div className="mt-3 flex justify-between text-xs text-[var(--muted)]"><span>Goal: 14 hours</span><span>68%</span></div></div></DashboardSection></aside></div></AppShell>;
}

function QuickStat({ label, value, detail }: { label: string; value: string; detail: string }) { return <div className="px-1 py-5 sm:px-5 sm:first:pl-0 sm:not-first:border-l sm:not-first:border-[var(--border)]"><p className="text-xs font-bold uppercase tracking-[0.12em] text-[var(--muted)]">{label}</p><div className="mt-2 flex items-baseline gap-2"><span className="text-2xl font-bold tracking-[-0.04em]">{value}</span><span className="text-xs text-[var(--muted)]">{detail}</span></div></div>; }
function DashboardSection({ title, icon, action, children }: { title: string; icon: React.ReactNode; action?: React.ReactNode; children: React.ReactNode }) { return <section><div className="mb-4 flex items-center justify-between"><h2 className="flex items-center gap-2 text-lg font-bold tracking-[-0.02em]"><span className="text-[var(--accent)]">{icon}</span>{title}</h2>{action}</div>{children}</section>; }
